<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_chats"
    app:startDestination="@id/pedidosList">

    <fragment
        android:id="@+id/pedidosList"
        android:name="com.kiwistudio.spelltrader.ui.chats.PedidosList"
        android:label="fragment_pedidos_list"
        tools:layout="@layout/fragment_pedidos_list" >
        <action
            android:id="@+id/action_pedidosList_to_chat"
            app:destination="@id/chat" />
    </fragment>
    <fragment
        android:id="@+id/chat"
        android:name="com.kiwistudio.spelltrader.ui.chats.Chat"
        android:label="fragment_chat"
        tools:layout="@layout/fragment_chat" />
</navigation>